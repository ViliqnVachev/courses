import java.util.Scanner;

public class GameOfIntervals {
    public static void main(String[] args) {


        System.out.println(Math.floor(5/2));
/*
        Scanner s = new Scanner(System.in);
        int hod = Integer.parseInt(s.nextLine());

        double firstInteral = 0.20;
        double seconInteral = 0.30;
        double thirdInteral = 0.40;
        double fourthInteral = 50;
        double fifthInteral = 100;

        double firstCount=0;
        double secondCount=0;
        double thirdCount=0;
        double fourthCount=0;
        double fifthCount=0;
        double invalidCount=0;

        double result = 0;
        double points = 0;
        for (int i = 0; i < hod; i++) {
            int a = Integer.parseInt(s.nextLine());
            if (a >= 0 && a <= 9) {
                result = a * firstInteral;
                points += result;
                firstCount+=1;

            } else if (a >= 10 && a <= 19) {
                result = a * seconInteral;
                points += result;
                secondCount+=1;
            } else if (a >= 20 && a <= 29) {
                result = a * thirdInteral;
                points += result;
                thirdCount+=1;
            } else if (a >= 30 && a <= 39) {
                result = fourthInteral;
                points += result;
                fourthCount+=1;
            } else if (a >= 40 && a <= 50) {
                result = fifthInteral;
                points += result;
                fifthCount+=1;
            } else {
                points=points/2;
                invalidCount+=1;
            }
            result=0;
        }
        System.out.printf("%.2f%n",points);

        System.out.printf("From 0 to 9: %.2f%%%n",firstCount/hod*100);
        System.out.printf("From 10 to 19: %.2f%%%n",secondCount/hod*100);
        System.out.printf("From 20 to 29: %.2f%%%n",thirdCount/hod*100);
        System.out.printf("From 30 to 39: %.2f%%%n",fourthCount/hod*100);
        System.out.printf("From 40 to 50: %.2f%%%n",fifthCount/hod*100);
        System.out.printf("Invalid numbers: %.2f%%",invalidCount/hod*100);

*/

    }
}
